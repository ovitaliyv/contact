<header class="row">
  <div class="span12">
    <div class="row">
      <div class="span3 locale_switcher">
      {{ locale_switcher() }}
      </div>
      <div>
      {% if app.user %}
        <a class="logout" href="{{ path('logout') }}"> (выход)</a> <span class="user_name">  <a class="logout" href="{{ path('currUser') }}">{{ app.user.username }} </a></span> 
      {% else %}
        <a class="login" href="{{ path('login') }}"> Вход</a>
      {% endif %}
      </div>
    </div>
      {% block header %}{% endblock %}
      {% include('::menu.html.twig') %}
  </div>
</header>
